defmodule Identicon.Image do
  # store list of numbers as the hex property
  # define struct with the properties 'hex', 'colour' and 'grid' with both default values as 'nil'
  defstruct hex: nil, colour: nil, grid: nil
end
